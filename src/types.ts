export type Property = {
  id: string;
  owner_id: string;
  title: string;
  description: string;
  price: number;
  city: string;
  postal_code: string;
  photos: string[];
  created_at: string;
};
